# トレカ価格トラッカー — Claude Code 向けの手順書

このリポジトリは、トレカBOXの買取価格を集めて、出現・消滅を検知するツールです（公開ページ: https://po-one-tcg.github.io/card-price-tracker/）。
詳しい仕組みは `README.md`。ユーザーはプログラミング初心者で、日本語で話します。**短く、実行結果（件数・検知）を見せて報告**してください。

## 毎日の作業: 買取コレクトの取り込み

ユーザーが、買取コレクトの **価格表の画像**（と、あれば価格更新の文章ポスト）を渡してきたら、次の手順で取り込みます。

### 1. 画像を、取り込み用のテキストに書き起こす
- 書式の見本: `scripts/box/fixtures/collect-2026-09-19.txt`（画像の全商品）と `collect-update.txt`（価格変更）。**この書式に完全に合わせる**。
- 見出しは次のとおり（`✅` で始める）。ここにない見出しは取り込まれない。
  - `✅コレクト ポケカ BOX` … 左の表（BOX / シュリンクつき / シュリンクなし）。**1商品を2行**にする: `商品名 19000円` と `商品名シュリ無 16000円`
  - `✅コレクト ポケカ その他` … 右の表（商品名と価格）
  - `✅コレクト ワンピース BOX・カートン` … 「未開封カートン」は `(カートン)OP-01 ロマンスドーン 〆切`、「未開封BOX」は `OP-01 ロマンスドーン 55000円`
  - `✅コレクト ワンピース その他` … プレミアムカードコレクション・その他（名前は見本に合わせて、プレミアムカードコレクションの商品には `プレミアムカードコレクション ` を頭に付ける）
  - `✅コレクト ドラゴンボール` / `✅コレクト 遊戯王`（遊戯王は `遊戯王 LIMIT OVER COLLECTION -THE HEROES- ボックス` / `… カートン`）
  - `✅コレクト ポケカシングルレート` … 右下のレート表（AR/CHR ¥160 など）。**取り込まない**ので、見出しと注記の1行だけ書く
  - `✅コレクト 価格変更` … 文章の価格更新ポスト。`商品名 金額円` の行にする（シュリンク無しは `商品名シュリ無 金額円`）。**複数あるときは、新しい（後の）ポストの値だけ**
- 表の `-`（ハイフン）は「買取なし」= `〆切`。金額は `19000円`（カンマ・¥なし）。
- **すべての行を書く**（`-` の商品も `〆切` で書く）。書き漏らした商品は「消滅」扱いになる（前回の6割未満だと取り込みが止まる）。
- 商品名は、画像の表記を基本に、見本にある名前があればそれに合わせる（他のお店と同じ商品として結び付けるため）。
- 画像が読みづらい所は、推測せずユーザーに聞く。書き起こしたら、**主な価格（数か所）をユーザーに見せて確認**してもらうと安心。

### 2. 取り込みを実行する
書き起こしたテキストを、一時ファイルに保存して（例: `/tmp/collect.txt`）、次を実行します。

```
git pull --rebase origin main
node scripts/box/import-text.js /tmp/collect.txt
node scripts/box/run.js --game none --no-images
node scripts/box/build-site.js
npm test
```

- `import-text.js` が「見出しを認識できません」と出したら、見出しの書き間違い。直して、やり直す。
- `run.js` の出力で、**`✕`（取り込めなかった区分）や `ERROR` が出たら、pushしない**。理由をユーザーに伝えて相談する。
  （前回の6割未満で止まった場合は、書き漏らしの可能性。確認して問題なければ `--confirm-drop` を付けて `import-text.js` をやり直す）
- 「今回の検知」の内容（🆕出現 / ✕消滅 / ⚠要確認）を、ユーザーに日本語で報告する。⚠要確認が出たら、管理画面（https://po-one-tcg.github.io/card-price-tracker/admin/）で判断してもらう。

### 3. 保存する
```
git add -A
git commit -m "コレクトを更新（画像の日付: ○月○日）"
git pull --rebase origin main
git push origin main
```
公開ページへの反映は、push の1〜2分後。

## 守ること
- **トークン（`github_pat_…` など）は、受け取らない・入力しない・コミットしない。** ユーザーがチャットに貼ってきたら、使わずに、削除して作り直すよう伝える。
- `data/`・`docs/`・`backups/` は、自動更新（GitHub Actions）も書き込む。**必ず `git pull --rebase` してから push** する。
- 依頼にない変更（コードの書き換えなど）はしない。
- 使用量が少ないことがある。余計な調査はせず、上の手順だけを実行する。

## 参考
- 店舗は5つ: BASE / 買取EXPO / コレクト / RUNTO / ソムリエ。BASE・RUNTO・ソムリエは自動取得、買取EXPOは管理画面にポストを貼る、コレクトはこの手順。
- 同じ商品の別表記は `config/product-aliases.json`（対応表）で1組ずつ統合する。
- テスト: `npm test`（72件）。
