# トレカ価格トラッカー — Claude Code 向けの手順書

このリポジトリは、トレカBOXの買取価格を集めて、出現・消滅を検知するツールです（公開ページ: https://po-one-tcg.github.io/card-price-tracker/）。
詳しい仕組みは `README.md`。ユーザーはプログラミング初心者で、日本語で話します。**短く、実行結果（件数・検知）を見せて報告**してください。

## 毎日の作業: 買取コレクトの取り込み

ユーザーが、買取コレクトの **価格表の画像**（と、あれば価格更新の文章ポスト）を渡してきたら、次の手順で取り込みます。

### 1. 画像を、取り込み用のテキストに書き起こす
- 書式の見本: `scripts/box/fixtures/collect-2026-09-19.txt`（画像の全商品）と `collect-update.txt`（価格変更）。**この書式に完全に合わせる**。
- 見出しは次のとおり（`✅` で始める）。ここにない見出しは取り込まれない。
  - `✅コレクト ポケカ BOX` … 左の表（BOX / シュリンクつき / シュリンクなし）。**1商品を2行**にする: `商品名 19000円` と `商品名シュリ無 16000円`
  - `✅コレクト ポケカ その他` … 右の表（商品名と価格）
  - `✅コレクト ワンピース BOX・カートン` … 「未開封カートン」は `(カートン)OP-01 ロマンスドーン 〆切`、「未開封BOX」は `OP-01 ロマンスドーン 55000円`
  - `✅コレクト ワンピース その他` … プレミアムカードコレクション・その他（名前は見本に合わせて、プレミアムカードコレクションの商品には `プレミアムカードコレクション ` を頭に付ける）
  - `✅コレクト ドラゴンボール` / `✅コレクト 遊戯王`（遊戯王は `遊戯王 LIMIT OVER COLLECTION -THE HEROES- ボックス` / `… カートン`）
  - `✅コレクト ポケカシングルレート` … 右下のレート表（AR/CHR ¥160 など）。**取り込まない**ので、見出しと注記の1行だけ書く
  - `✅コレクト 価格変更` … 文章の価格更新ポスト。`商品名 金額円` の行にする（シュリンク無しは `商品名シュリ無 金額円`）。**複数あるときは、新しい（後の）ポストの値だけ**
- 表の `-`（ハイフン）は「買取なし」= `〆切`。金額は `19000円`（カンマ・¥なし）。
- **すべての行を書く**（`-` の商品も `〆切` で書く）。書き漏らした商品は「消滅」扱いになる（前回の6割未満だと取り込みが止まる）。
- 商品名は、画像の表記を基本に、見本にある名前があればそれに合わせる（他のお店と同じ商品として結び付けるため）。
- 画像が読みづらい所は、推測せずユーザーに聞く。書き起こしたら、**主な価格（数か所）をユーザーに見せて確認**してもらうと安心。

### 2. 取り込みを実行する
書き起こしたテキストを、一時ファイルに保存して（例: `/tmp/collect.txt`）、次を実行します。

```
git pull --rebase origin main
npm ci
node scripts/box/import-text.js /tmp/collect.txt
node scripts/box/run.js --game none --no-images
node scripts/box/build-site.js
npm test
```

- `import-text.js` が「見出しを認識できません」と出したら、見出しの書き間違い。直して、やり直す。
- `run.js` の出力で、**`✕`（取り込めなかった区分）や `ERROR` が出たら、pushしない**。理由をユーザーに伝えて相談する。
  （前回の6割未満で止まった場合は、書き漏らしの可能性。確認して問題なければ `--confirm-drop` を付けて `import-text.js` をやり直す）
- 「今回の検知」の内容（🆕出現 / ✕消滅 / ⚠要確認）を、ユーザーに日本語で報告する。⚠要確認が出たら、管理画面（https://po-one-tcg.github.io/card-price-tracker/admin/）で判断してもらう。

### 3. 保存する
**作業用ブランチ（`claude/...` など）にいることがある。その場合、そこにcommit/pushしただけでは公開ページに反映されない（公開ページは常に `main` から作られる）。必ず `main` に反映するところまで行う。**

```
git add -A
git commit -m "コレクトを更新（画像の日付: ○月○日）"
git branch --show-current
```
- 出力が `main` なら、そのまま:
  ```
  git pull --rebase origin main
  git push origin main
  ```
- 出力が `main` 以外（例: `claude/...`）なら、そのブランチをpushしたうえで、さらに `main` に反映する:
  ```
  git push -u origin <今のブランチ名>
  git checkout main
  git pull --rebase origin main
  git merge --ff-only <今のブランチ名>
  git push origin main
  ```
- 最後に `git log origin/main -1` で、今回のコミットが `main` の先頭に入っていることを確認してから、ユーザーに報告する。

公開ページへの反映は、`main` へのpushの1〜2分後。

## 毎日の作業: 買取EXPO・にこにこの取り込み（管理画面を使わない方法）

管理画面に貼り付ける代わりに、ユーザーがチャットに **Xのポストのテキスト（またはスクリーンショット）**、にこにこは **買取価格表のコピー** を貼ってきたら、コレクトと同じ手順（上の2・3章）で取り込めます。
- EXPO: `✅ポケカ Box` などの見出し付きのテキストは、そのまま `node scripts/box/import-text.js` に渡せる（書式の見本: `scripts/box/fixtures/expo-*.txt`）。画像なら、この書式に書き起こす。「価格変更のお知らせ」も同じファイルに入れてよい。
- にこにこ: 「ポケモンカード / 64商品」のようなページ形式は、そのまま渡せる（見本: `scripts/box/fixtures/nikoniko-page-2026-09-26.txt`）。
- `run.js` の出力で `✕` や `ERROR` が出たら、pushせずに相談する（コレクトと同じ）。

## 過去の日付ぶんを遡って入力する

買取EXPO・コレクトは過去の投稿（画像・テキスト）が残っていれば、**新しい日から古い日へ、どの順番で何回に分けて入れても大丈夫**です。
`node scripts/box/import-history.js` は、通常の取り込み（`import-text.js` → `run.js`）とは別物で、**「今の価格」や「出現・消滅」の判定には一切触れず、その日の履歴（グラフ）にだけ追加**します。だから古い日を後から差し込んでも、最新の価格と比べて誤検知したり上書きしたりしません。

1. その日の画像を、通常と同じ書式でテキストに書き起こす（1章と同じ。ただし「価格変更のお知らせ」区分は対象外）
2. 一時ファイルに保存して実行:
   ```
   git pull --rebase origin main
   node scripts/box/import-history.js /tmp/collect-過去日付.txt 2026-09-15
   ```
   （日付は画像の日付。時刻を指定したいときは `--time 13:00` を付ける）
3. 出力で `✓ 区分名: N件` を確認。`商品マスタに無いため対象外` と出た商品は、まだ一度も取り込んでいない名前なので無視してよい（通常の当日分の取り込みを先に済ませておくと減る）
4. 複数日ぶんまとめて 2〜3 を繰り返してから、最後にまとめて:
   ```
   node scripts/box/build-site.js
   npm test
   ```
5. 保存は3章と同じ手順（`main` に反映されたか確認するところまで）

## 3ヶ月に1回の作業: フラッグシップバトルの配布枚数

ワンピースカードゲーム「フラッグシップバトル」の記念品カード配布枚数（推定）ページ（公開ページの「フラッグシップ配布枚数」リンク）。
新しい回になったら、ユーザーから次の3つを聞く: ①その回の3つのシリーズURL（`https://optcg-port.com/series/xxxx`）、②記念品カード名・型番（優勝／ベスト8）、③配布条件（定員→枚数。通常は32人開催の枚数がわかれば、64人はその2倍）。

1. `C:\optcg` でデータ取得（Playwrightがすでにセットアップ済み。別リポジトリなので `cd` して実行）:
   ```
   cd C:/optcg && node scrape.js <ID1> <ID2> <ID3>
   ```
2. card-price-tracker 側に取り込む（店舗名・住所など不要な項目を落として軽くする）:
   ```
   cd C:/card-price-tracker
   node scripts/flagship/import.js <ID1> <ID2> <ID3>
   ```
3. `config/flagship.json` に新しい回を追記する（`rounds` 配列の先頭に追加。過去の回は消さない）。`series`・`prizes`（カード名・型番・`rules`＝定員ごとの配布枚数）をユーザーから聞いた内容で埋める。
4. 集計してページ用データを作る:
   ```
   node scripts/flagship/build.js
   ```
   出力される推定枚数をユーザーに見せて確認してもらう。
5. 保存は3章と同じ手順（`main` に反映されたか確認するところまで）。

## 守ること
- **トークン（`github_pat_…` など）は、受け取らない・入力しない・コミットしない。** ユーザーがチャットに貼ってきたら、使わずに、削除して作り直すよう伝える。
- `data/`・`docs/`・`backups/` は、自動更新（GitHub Actions）も書き込む。**必ず `git pull --rebase` してから push** する。
- 依頼にない変更（コードの書き換えなど）はしない。
- 使用量が少ないことがある。余計な調査はせず、上の手順だけを実行する。

## 参考
- 店舗は5つ: BASE / 買取EXPO / コレクト / RUNTO / ソムリエ。BASE・RUNTO・ソムリエは自動取得、買取EXPOは管理画面にポストを貼る、コレクトはこの手順。
- 同じ商品の別表記は `config/product-aliases.json`（対応表）で1組ずつ統合する。
- テスト: `npm test`（107件）。
- フラッグシップバトルのページは、価格トラッカーとは別データ（`config/flagship.json`・`data/flagship/`・`docs/flagship.html`）。自動更新（GitHub Actions）の対象外で、3ヶ月に1回、上の手順で手動更新する。
